# 🚀 자동 성능 측정 가이드

이 도구를 사용하면 Lighthouse를 수동으로 실행하지 않고도 성능을 자동으로 측정하고 비교할 수 있습니다.

## 📦 설치

```bash
# 의존성 설치 (처음 한 번만)
npm install
```

## 🎯 사용법

### 1단계: 개선 전 성능 측정 (Baseline)

먼저 코드를 수정하기 전 현재 상태를 측정합니다.

```bash
# 터미널 1: 개발 서버 시작
npm run dev

# 터미널 2: 개선 전 성능 측정
npm run measure:before
```

결과 예시:

```
🚀 웹 성능 자동 측정 도구

🔄 3회 측정 시작...
   실행 1/3...
   실행 2/3...
   실행 3/3...
✅ 측정 완료

📊 성능 측정 결과 (중간값)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Performance Score: ❌ 52/100

  Core Web Vitals:
    LCP: ❌ 4.52s (목표: < 2.5s)
    FCP: ❌ 2.43s (목표: < 1.8s)
    CLS: ❌ 0.345 (목표: < 0.1)

  기타 지표:
    Speed Index: 5.12s
    TTI: 6.21s
    TBT: ❌ 456ms (목표: < 200ms)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 개선 전 기준선(baseline) 저장 완료

💡 이제 코드를 개선한 후 `npm run measure:compare`로 비교하세요.
```

이제 `lighthouse-results/before.json`에 기준선이 저장되었습니다.

---

### 2단계: 코드 개선

assignment.md를 참고하여 코드를 개선합니다.

---

### 3단계: 개선 후 성능 비교

코드를 수정한 후, 개선 전과 비교합니다.

```bash
# 개발 서버가 실행 중인 상태에서
npm run measure:compare
```

결과 예시:

```
🚀 웹 성능 자동 측정 도구

🔄 3회 측정 시작...
✅ 측정 완료

📊 개선 전 (Before)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Performance Score: ❌ 52/100
  Core Web Vitals:
    LCP: ❌ 4.52s
    FCP: ❌ 2.43s
    CLS: ❌ 0.345
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 개선 후 (After)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Performance Score: ✅ 93/100
    (📈 +41)

  Core Web Vitals:
    LCP: ✅ 2.15s (목표: < 2.5s)
        (⬇️ -2.37s)
    FCP: ✅ 1.62s (목표: < 1.8s)
        (⬇️ -0.81s)
    CLS: ✅ 0.045 (목표: < 0.1)
        (⬇️ -0.300)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📈 개선 현황
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Performance: 52 → 93 (+78.8%)
  LCP: 4.52s → 2.15s (↓52.4%)
  FCP: 2.43s → 1.62s (↓33.3%)
  CLS: 0.345 → 0.045 (↓87.0%)
  TBT: 456ms → 178ms (↓61.0%)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 목표 달성 체크
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✅ Performance ≥ 90
  ✅ LCP < 2.5s
  ✅ FCP < 1.8s
  ✅ CLS < 0.1
  ✅ TBT < 200ms

  달성률: 5/5 (100%)

  🎉 축하합니다! 모든 목표를 달성했습니다!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

### 4단계: 단순 측정 (비교 없이)

기준선 없이 현재 상태만 측정하고 싶다면:

```bash
npm run measure
```

---

## 📁 결과 파일

모든 측정 결과는 `lighthouse-results/` 폴더에 저장됩니다:

```
lighthouse-results/
├── before.json              # 개선 전 기준선
├── result-2024-01-28T...json  # 각 측정 결과
└── result-2024-01-28T...json
```

각 JSON 파일에는 다음 정보가 포함됩니다:

- 주요 성능 지표 (Performance, LCP, FCP, CLS, TBT 등)
- 3회 측정의 모든 원본 데이터

---

## 🔧 문제 해결

### 오류: "로컬 서버가 실행되지 않았습니다"

```bash
# 다른 터미널 탭에서 개발 서버 실행
npm run dev
```

### 오류: "lighthouse is not defined" 또는 모듈 오류

```bash
# 의존성 재설치
rm -rf node_modules package-lock.json
npm install
```

### Chrome이 설치되지 않았다는 오류

Lighthouse는 Chrome/Chromium이 필요합니다. Chrome 브라우저를 설치하세요.

### 측정 시간이 너무 오래 걸림

- 3회 측정은 약 1~2분 정도 소요됩니다.
- 백그라운드에서 다른 프로그램을 종료하면 더 정확하고 빠릅니다.

---

## 💡 팁

### 정확한 측정을 위해

1. **다른 탭/프로그램 종료**: CPU/메모리 영향 최소화
2. **VPN 끄기**: 네트워크 일관성 유지
3. **충전 중**: 노트북 절전 모드 방지

### 측정 주기

- **각 Step 완료 후** 측정
- **큰 변경 전/후** 비교
- **최종 제출 전** 확인

### 보고서 작성용

측정 결과를 스크린샷으로 캡처하거나, JSON 파일을 보고서에 첨부하세요.

---

## 📊 목표 지표 (다시 확인)

| 지표        | 목표    | 현재 | 상태 |
| ----------- | ------- | ---- | ---- |
| Performance | ≥ 90    | ?    | ?    |
| LCP         | < 2.5s  | ?    | ?    |
| FCP         | < 1.8s  | ?    | ?    |
| CLS         | < 0.1   | ?    | ?    |
| TBT         | < 200ms | ?    | ?    |

---

## ❓ 자주 묻는 질문

**Q1. 매번 점수가 다르게 나와요**

A. 정상입니다. 네트워크 상태에 따라 ±3~5점 변동될 수 있습니다. 그래서 3회 측정 후 중간값을 사용합니다.

**Q2. 목표를 못 달성했어요**

A. assignment.md의 각 Step을 다시 확인하고, 힌트를 참고하세요. 멘토에게 질문해도 됩니다.

**Q3. 일부 지표만 개선하고 싶어요**

A. Step별로 집중하는 지표가 다릅니다:

- Step 2: LCP
- Step 3: FCP
- Step 4: TTI, TBT
- Step 5: CLS
- Step 6: INP (측정되지 않음, 수동 테스트 필요)

---

**Good luck! 🚀**
